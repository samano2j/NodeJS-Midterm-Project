module.exports = {
  content: ["./src/**/*.{html,js}", './src/**/*.ejs', "./views/**/*.ejs"], // Specify the files to scan for classes
  theme: {
    extend: {}, 
  },
  variants: {}, 
  plugins: [
  ], 
};